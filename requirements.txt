# ── API framework ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# ── NLP / ML core ─────────────────────────────────────────────────────────────
spacy>=3.7.0,<3.9.0
# torch installed separately in build.sh (CPU-only wheel)
sentence-transformers>=2.6.0,<4.0.0
scikit-learn>=1.4.0
numpy>=1.26.0,<2.0.0        # numpy 2.x breaks some spaCy internals
rank-bm25>=0.2.2
rapidfuzz>=3.6.0

# ── Gemini (new unified SDK) ──────────────────────────────────────────────────
# Imports as: from google import genai
# DO NOT add google-generativeai — it conflicts with this package.
google-genai>=0.7.0

# ── Async HTTP (used for fallback if needed) ───────────────────────────────────
httpx>=0.27.0

# ── Memory diagnostics (optional but useful on Render) ────────────────────────
psutil>=5.9.0

# ── PDF text extraction (for /extract endpoint if added later) ────────────────
pdfplumber>=0.11.0
python-multipart>=0.0.9
